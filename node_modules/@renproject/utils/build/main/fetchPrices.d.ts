import { Logger } from "@renproject/interfaces";
import BigNumber from "bignumber.js";
import { OrderedMap } from "immutable";
export declare const getTokenPrices: (tokens: string[], logger?: Logger | undefined) => Promise<OrderedMap<string, number>>;
export declare type TokenPrices = OrderedMap<string, number>;
export declare const normalizeValue: (prices: OrderedMap<string, number>, token: string, value: string | number | BigNumber) => BigNumber;

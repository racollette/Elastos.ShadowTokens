import { RenNetworkDetails } from "@renproject/contracts";
import { BurnAndReleaseEvent, LockAndMintEvent, LockAndMintParams } from "@renproject/interfaces";
export declare const GATEWAY_ENDPOINT_STAGING = "https://gateway-staging.renproject.io/";
export declare const GATEWAY_ENDPOINT_PRODUCTION = "https://renproject.github.io/gateway-staging.renproject.io/";
export declare const getElement: (id: string) => HTMLElement;
export declare const createElementFromHTML: (htmlString: string) => ChildNode | null;
export declare const resolveEndpoint: (endpointIn: string, network: RenNetworkDetails, pathIn: string, transferID?: string | undefined) => string;
/**
 * prepareParamsForSendMessage turns possible BigNumber values into strings
 * before passing the params to sendMessage.
 * The error message `can't clone ...` is thrown if this step is skipped.
 * @param transferParamsIn The parameters being fixed.
 */
export declare const prepareParamsForSendMessage: (transferParamsIn: LockAndMintParams | import("@renproject/interfaces").BurnAndReleaseParams | LockAndMintEvent | BurnAndReleaseEvent) => LockAndMintParams | import("@renproject/interfaces").BurnAndReleaseParams | LockAndMintEvent | BurnAndReleaseEvent;
